import Todo from "../models/todos.models";
import User from "../models/user.models";
import { errorHandler } from "../utils/error";



const createsubtodo = async(req, res, next) => {
    try {
        const { coontent, complete } = req.body;

    } catch (error) {

    }
}